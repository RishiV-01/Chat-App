# Server
PORT=3001
LOG_LEVEL=info

# Database (MongoDB or AWS DocumentDB)
# For DocumentDB, append TLS params: ?tls=true&tlsCAFile=/app/certs/global-bundle.pem&retryWrites=false
MONGODB_URI=mongodb://localhost:27017/chat_app_db

# CORS — allowed origin for frontend
CORS_ORIGIN=http://localhost:5173

# JWT Secret for internal authentication tokens
JWT_SECRET=change-this-to-a-secure-random-string

# E3 Files API (required for file uploads)
E3_FILES_API_BASE=https://e3-auth-api.thor.healthintech.net/

# Maximum file upload size in bytes (default: 100MB)
MAX_FILE_SIZE=104857600

# E3 Auth API (optional — enables parent app token exchange)
# AUTH_API_BASE=https://e3-auth-api.thor.healthintech.net/
# AUTH_API_APP_NAME=e3_messaging

# Parent App Origin (optional — enables iframe embedding with postMessage)
# PARENT_APP_ORIGIN=https://parent-app.example.com

# Seed database with sample data (for initial testing only)
SEED_ON_START=false
